import { ScreenStub } from "@/components/ScreenStub";

// A4 — Sell (cart). Built in M3.
export default function SellPage() {
  return <ScreenStub titleKey="navSell" note="Cart, checkout & receipt arrive in M3." />;
}
